from setuptools import setup, find_packages

setup(
    name="mottaipy",
    version='0.0.0',
    description="the mottai game, implemented in python",
    license="MIT",
    packages=find_packages(),
    install_requires=[
    ],
    dependency_links=[
    ],
)
